server:
  port: 8088
spring:
  application:
    name: happraisal-service
  datasource:
    url: jdbc:postgresql://localhost:5432/happraisaldb
    driverClassName: org.postgresql.Driver
  jpa:
    open-in-view: false
    generate-ddl: false
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
        hibernate:
            format_sql: true

management:
  server:
    port: 9999
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS